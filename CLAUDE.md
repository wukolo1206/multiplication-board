---
project: 乘法直式板（四上第二單元）
category: 學科工具集
status: 開發中
version: "v1.0 五頁完成，核心 74 項＋端到端 52 項全過 2026-08-23"
url: https://wukolo1206.github.io/multiplication-board/
next_action: 建 GitHub repo 並 push，開啟 Pages，再做實機驗收（教師大屏＋學生 iPad）
updated: 2026-08-23
---

# CLAUDE.md — 乘法直式板

康軒數學**四上第二單元「整數的乘法」**的互動教具。純 HTML + CSS + 原生 JS，無框架、無 build。

**設計依據**：`../docs/superpowers/specs/2026-08-23-u2-multiplication-design.md`（v1.2，經兩輪交叉審查）

## 頁面

| 檔案 | 內容 | 課本 |
|---|---|---|
| `index.html` | 導覽 | — |
| `steps.html` | 直式的每一步（一千元紙鈔、四式並列、逐步揭示） | p20–21 活動一 |
| `array.html` | 方格圖與直式（三階段：四列 1:1 → 主動合併 → 兩列 2:1） | p24–25 活動二問5–6 |
| `carry.html` | 進位不要漏（逐位診斷、列層級診斷、L1/L2/L3） | p21、p25–28 |
| `fix.html` | 找出直式的錯（四關，第四關是列式結構） | p29 練習百分百(二) |
| `parcel.html` | 文旦運費費率表（兩步驟：查表＋乘法） | p19 開門活動 |

每頁三分頁：🔍探索（教師觸控大屏）／✏️練習（學生 iPad）／🏆挑戰（學力檢測考古題）。

## 核心邏輯

`multiplication-core.js` —— **網頁與測試共用同一份，不可搬回 HTML 內嵌**（那會讓測試變成測複本）。

診斷演算法有三件事不能亂改，改了要重跑 `python run-tests.py`：

1. **寫入格與進位格要一起判定。** 只看寫入格時，「漏進位」與「先加後乘」有 80 組碰撞。
2. **被乘數該位為 0 時不設兩個代碼。** `(0＋進位)×乘數` 與 `進位×乘數` 是同一個算式，
   分不開，合併為 `ZERO_DIGIT_CARRY_CONFUSION`。硬用判定順序去分等於替學生亂下斷言。
3. **`carryIn > 0` 才做進位診斷，且比對的是寫入的個位數不是完整乘積。**

`AMBIGUOUS_CARRY` 是守門分支，窮舉 80,000 組後出現 0 次。**如果它真的觸發了，代表規格被改過，要回頭重驗。**

## 教學內容的規矩

- **位值敘述、中文說法一律照課本原文**，不要憑語感改寫。`steps.html` 的四句就是課本 p21 原文。
- **具體物層級**：3000 元是「3 張一千元」，**不是「3 萬元」**。U1 在這裡出過錯。
- **進位記在哪裡有三種說法**（課本 p21 上方／課本 p25 教師註記／教師手冊 印刷頁27 下方），
  UI 預設用課本 p21 的上方並可切換，**不可宣稱單一寫法是課本明訂**。
- **考古題選項一律照考卷原題**，不可自己算誘答填進去。`111-2` 的直式依原題本 PDF 重現。
- **超出單元範圍的考古題標示、不刪除**（`parcel.html` 的 109-13 複名數）。

## 能宣稱什麼、不能宣稱什麼

- `array` / `carry` / `fix` —— 針對三項**教師手冊明文**的學生困難（位值對齊、遺漏進位、列式對調）
- `steps` / `parcel` —— **只是課本內容的數位化練習**，不可寫成「補救迷思概念」
- L1/L2/L3 是**協助遞減（fading）**，**不叫鷹架** —— 它沒有依學生表現調整協助的診斷環節

## 測試

```bash
python run-tests.py
```
- `multiplication-core.test.js` 74 項，期望值全取自課本標準答案並標明頁碼
- `e2e.spec.py` 六頁端到端 52 項（自動起 http server）
- **不能用 `file://` 開**，頁面用 `<script src>` 載入 core

## 部署

GitHub Pages（`main` / root）。上層 repo `D:\備課ai` 以 submodule 登記。
**子 repo push 成功、確認遠端有該 SHA 之後，才能把上層轉 gitlink。**
