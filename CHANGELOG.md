# CHANGELOG — 乘法直式板

## @2026-09-13 — v1.1.8　緊湊進位空間（密一點更符合實際紙筆直式運算）

- **進位空間大幅收緊、消除多餘空隙**：
  - 依照使用者指示：「這個進位的空間可以密一點嗎，比較符合實際運算」。
  - 徹底修正進位列中 `place-spacer` 繼承 `.practice-cell` 預設 56px 高度的問題，新增 `.mini-carry-cell.place-spacer`，將進位行高精確壓縮至 15px~20px。
  - 第一列與第二列之間的間隙由原先 64px 縮減至約 18px，第二列小進位 `⑤` 緊貼數字頭頂；加法進位 `+1` 緊靠第一列上面的直線下緣，視覺比例高度貼近真實黑板與紙筆手算直式。
- **全套測試 100% 通過**：核心 116 項與六頁端到端測試全部通過。

## @2026-09-13 — v1.1.7　直式進位三大位置分離（第二列上方小進位、相加進位於直線處）與全程保留進位

- **第二行的進位顯示在第二列數字的上面，且更小巧（`.mini`）**：
  - 依使用者指示：「第二行的進位能否在第二列數字的上面，然後小一點，請都保留進位，最後相加的進位，請在第一列上面的直線那邊」。
  - 第二列乘十位的乘法進位（Phase 1，`r1`，如 $8 \times 7 = 56$ 的進位 $5$）獨立配置於「第一列與第二列之間」的緊湊進位列中，直接浮現在第二列數字頭頂，尺寸縮小至 18px，字體 11px，小巧精緻不佔版面。
- **最後相加的進位記在第一列上面的直線處**：
  - 兩排相加時產生的加法進位（Phase 2，`add`，如 $6 + 5 = 11$ 的進位 `+1`）移至「第一列數字上方緊貼橫線 1 處」（即第一列上面的直線那邊），完全符合加法運算時將進位記在被加數（第一列）上方直線的數學紙筆書寫習慣。
- **全程保留進位（不再覆蓋或消失）**：
  - 各階段產生的進位（被乘數上方 Phase 0 進位、第二列上方 Phase 1 進位、直線處 Phase 2 加法進位）在步驟推進、切換階段以及答對完成（`isDone`）後**全數保留顯示**；已使用進位轉為帶刪除線的淡化樣式，完整保留學生全題的進位思維脈絡。
- **全套測試 100% 通過**：核心 116 項與六頁端到端測試全部通過。

## @2026-09-13 — v1.1.6　被乘數整體框起（表示重複計算）與當前位底色強調

- **被乘數整體框起來（一體化連續外框）**：
  - 依照使用者教學理念指示：「上面的被乘數請一起框起來，表示現在是 52 要重複 4 次的計算；正在算的才用底色；這邊（二三位數×整十）也都用相同的方式表示」。
  - 在兩列直式（`steps.html` 與 `array.html`）及「二三位數×整十」（`h3`）中，將被乘數各個位數（如 $52$、$42$、$375$）以連續一體化圓角琥珀金外框（`.factor-a-group`）包裹在一起：
    - 左端最高位（`group-start`）：封閉左邊框與左側 10px 圓角。
    - 中間位（`group-mid`）：上下邊框貫通無中斷。
    - 右端個位（`group-end`）：封閉右邊框與右側 10px 圓角。
    - 單一位數（`group-single`）：全包圍 10px 圓角。
- **正在算的那一位才用底色**：
  - 微步模式下，在被乘數整組外框內，僅有當前微步正在計算的數位（如 $2 \times 4$ 中的個位 $2$）亮起鮮黃底色（`#fef08a`，`.active-calc`）；未參與當前計算的高位（如十位 $5$）保持淡白底色，視線焦點清晰聚焦。
  - 在「二三位數×整十」直式中，被乘數 $42$ 整體框為一體，乘數十位 $6$ 與固定個位 $0$ 清楚對齊，直觀呈現「$42$ 要重複 $6$ 個十」的核心位值概念。
- **全套測試 100% 通過**：核心 116 項與六頁端到端測試全部通過。

## @2026-09-13 — v1.1.5　兩列直式微步驟拆解（按一下一微步）、進位符號視覺化與非焦點字調亮

- **按一下就是一個微步驟（含進位符號）**：
  - 兩列直式練習全面升級微步拆解（`C.multiDigitSteps(a, b)`），支援「個位乘個位 $\to$ 十位乘個位 $\to$ 個位乘十位 $\to$ 十位乘十位 $\to$ 兩排逐位相加」。
  - 每按一下填入一個數字，直式定位板專屬配置「進位列」（`.carry-cell`）：
    - 第一列進位：橘黃色徽章（`r0`，如 `①`、`④`），加完後轉為帶刪除線的 `.used`。
    - 第二列進位：藍紫色徽章（`r1`）。
    - 兩排相加進位：珊瑚紅徽章（`add`，如 `+1`）。
  - 當前相乘的被乘數位與乘數位獨立亮起，直式目標格以紫色虛線框動態鎖定。
  - 右側引導卡與下方按鈕同步顯示精細微步指引（如「👉 點我填入：個位寫 2，十位記進位 1」）。
- **乘數非焦點灰色數字調亮**：
  - 乘數未參與當前計算的數字由原本過暗的 `opacity: 0.2`（灰色）調亮至 `opacity: 0.5`（`#64748b`），大螢幕與平板上清晰可讀，同時維持主從焦點層次。
- **雙模式自由切換**：
  - 提供 `[ 👣 逐位算（含進位） ]`（預設，按一下就是一個微步並顯示進位）與 `[ ⚡ 整列算 ]`（快速模式，一次填入整列），同步支援 `steps.html` 與 `array.html`。
- **測試全部通過**：核心 116 項與六頁端到端測試全部 100% 通過。

## @2026-09-13 — v1.1.4　steps.html 一鍵送出位值觀察與兩列直式分步練習分頁整合

- **二三位數×整十落實一鍵送出觀察體驗**：
  - 依照使用者需求「點一下就可以送出答案，主要是要觀察乘以整十，個位固定是0，從十位開始計算」。
  - 點擊直式虛線答案格、右側步驟引導卡片或下方大按鈕即可一鍵填入答案並判定答對，不需鍵盤打字。
  - 答對後直式答案格亮綠、按鈕切換為「換下一題 →」，同時保留「鍵盤輸入（選用）」供進階測驗。
  - 頂部設口語引導條、底部設位值等式條，採垂直居中堆疊排版徹底防止寬度溢出。
- **直式每一步整合「✏️ 兩列直式」獨立分頁**：
  - 分頁列標籤更新為「✏️ 兩列直式」，7 個分頁完美等寬單行排版。
  - 完整移植 `array.html` 兩列直式練習：一位至四位數被乘數選擇、口語提示句、當前乘數高亮、另一個數字變暗、第一列/第二列/合計三步點選填入、重複次數等式。
  - 支援雙模式切換：保留「🎯 分步點選練習」與「📖 課本步驟演示」（原 6×28、24×12、28×74 逐位拆解演示）。
- **全套測試 100% 通過**：核心 116 項與六頁端到端測試全部通過。

## @2026-09-13 — v1.1.3　steps.html 新增「二三位數×整十」無限練習與定位強調（0固定在個位）

- **新增「🔢 二三位數×整十」分頁**：位於「整十×整十」與「乘以二位數」之間，提供無限多題的二位數×整十（如 42×60、53×80）與三位數×整十（如 605×80、125×40）。
- **特別強調直式位置與 0 固定在個位**：
  - 被乘數每一位數與乘數十位數以金黃圓角框線與光暈顯著強調（`.highlight-factor`）。
  - 乘數個位 0 固定在個位格，右側標記「被乘數」與「乘數」位值身分標籤。
  - 答案列個位專屬固定顯示 `0`（帶「固定0」定位標記），清楚傳達「乘數是整十數時個位固定記 0，只要計算被乘數×十位數（幾個十）對齊十位往左寫」的核心位值概念。
  - 右側動態引導卡即時呈現每一步思考與算式（未作答提示、輸入提示、答對確認）。
- **無限出題與題型篩選**：提供「隨機混合」、「二位數 × 整十」、「三位數 × 整十」快捷切換，前幾題包含課本經典示範題（42×60、53×80、605×80），後續無限隨機出題。
- **全方位位值診斷**：自動診斷答對、漏補 0、補多 0、乘法計算錯誤，給予最精確的教學指引。
- 核心單元測試擴充至 116 項，端到端測試擴充至 58 項，全數 100% 通過。

## @2026-09-13 — v1.1.2　array.html 步驟式高亮、變暗與點選填入（一次強調一個重點）

- **強調第一步要乘的**：被乘數每一位與乘數個位數在第一步以亮黃金框與微光暈顯著強調；第二步時換成乘數十位數高亮。
- **乘數另一個數字變暗**：第一步時乘數十位數變暗（淡灰低對比度）；第二步時乘數個位數變暗，視線完全不被干擾。
- **文字出現在各列旁邊**：直式 Grid 引入右側引導說明列，第一列的算式與引導緊貼第一列右側；第二列引導與提醒緊貼第二列右側；合計引導緊貼合計列右側，高度完全水平對齊。
- **直接點選不需鍵盤打字**：取消 `<input>` 輸入框，改為點選式答案格與引導按鈕，學生或教師點選右側按鈕或直式格子即可直接填入部分積與總和，適合 iPad 與觸控大螢幕教學。
- **一次強調一個重點**：分步鎖定推進（第一步個位列 → 第二步十位列 → 第三步兩排相加 → 答對加總確認），上方口語提示句與下方重複次數等式同步動態對齊焦點。
- 核心測試 106 項與端到端 52 項全部通過。

## @2026-09-13 — v1.1.1　定位板格線對齊修正

- 練習直式改用真正的百位／十位／個位共用定位板 Grid，不再用固定寬度輸入框模擬左移。
- 第一列、第二列與最後答案改為每格填一個數字，所有列共用相同 `data-pos` 欄位，第二列的個位 0 能自然落在個位格。
- 已推送至 GitHub Pages，線上頁面確認新版內容。

## @2026-09-13 — v1.1　array.html 無限兩列部分積練習

- 練習頁改為可無限產生新題，完成後可持續按「下一題」並累計題數。
- 被乘數可選一至四位數，預設兩位數；乘數固定為十位與個位皆非 0 的兩位數。
- 放大兩列直式輸入區：第一列使用個位重複次數，第二列使用實際十位值（×10、×20…）並對齊十位。
- 畫面同步顯示口語記憶句與「個位＋十位×10＝乘數」重複次數等式，答錯時提供十位位值提示。
- 核心測試 106 項通過；Node Playwright 互動驗收涵蓋桌面與 390px 窄版。
- 已推送至 GitHub Pages：`https://wukolo1206.github.io/multiplication-board/`，線上內容驗證為新版。

## @2026-08-23 — v1.0　五頁完成

- 核心邏輯 `multiplication-core.js`：逐位展開、部分積與左移、面積模型四區塊、
  逐位診斷、列層級診斷、錯誤直式分類
- 五個頁面 ＋ 導覽：`steps` / `array` / `carry` / `fix` / `parcel`
- 測試 `python run-tests.py`：核心 74 項、端到端 52 項，全部通過
- 考古題四題全部使用**考卷原始選項**；111-2 的直式依原題本 PDF 重現
- 設計文件 v1.2 經兩輪交叉審查（共 23 條，採納 17、部分採納 6、駁回 0）

### 這一版特別處理的三件事
1. 進位標記位置：課本 p21 說上方、教師手冊說下方，UI 預設上方並可切換，兩種說法都列出
2. 診斷演算法：修掉一個 100% 死碼，改用進位格消歧義而非判定順序
3. `array.html` 用**表徵提示遞減**，與 `carry.html` 的計算支持遞減分開
