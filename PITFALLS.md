# PITFALLS — 乘法直式板

## 已知風險（尚未處理）
- **尚未實機驗收**：教師觸控大屏與學生 iPad 的實際手感（拖曳靈敏度、字級、48px 是否真的夠大）
- **L3 無協助正確率才是有效性的證據**，目前沒有收集機制，只能靠老師現場觀察
- `array.html` L2「只有外框」把標號也拿掉了，若現場發現學生完全無從下手，
  再考慮保留標號但拿掉面積數字

---

## P1　診斷規則「依序比對」會出現死碼
第一版寫了五條規則依序比對，其中一條是
`＝ 進位×乘數`（被乘數該位為 0 時用）。
但該位為 0 時 `(0＋進位)×乘數` **恆等於** `進位×乘數`，
永遠先命中前一條 → **那條規則 100% 不可達**。

**教訓**：凡是「依序比對」的規則集，寫完就要窮舉跑一次，
問：**有沒有兩條在某些輸入下等價？** 光看文字讀不出來。
測試第 5b 組已把「每個代碼都要可達」寫成永久回歸測試。

## P2　比對完整乘積 vs 比對寫入的個位數
直式格子裡填的是**寫下的那一位數字**（6×3=18 填 8），
但規則寫成 `輸入 === 該位數字×乘數`（＝18），型別不符，診斷全面失效。
**寫診斷前先確定「學生到底填什麼進去」。**

## P3　改了版面就要回頭檢查判定邏輯
把二位數以上改成列層級輸入之後，五條診斷規則仍然是逐位的，對不上。
**只改觸發處、沒改底層演算法** —— U1 也踩過同一種坑。

## P4　一套機制套到第二個頁面前，先問會不會摧毀那一頁的目的
L1/L2/L3 對 `carry.html` 是對的，照抄到 `array.html` 會讓它失去面積模型的意義。
共用的是「協助遞減」這個**原則**，不是同一張表。

## P5　教材頁碼要雙標（印刷頁＋PDF頁）
教師手冊印刷頁 ＝ PDF 頁 − 2；課本印刷頁 ＝ PDF 頁 + 18。
只寫一種就標錯過：注意事項二與三其實在印刷頁 27，不是 26。

## P6　「課本說」與「教師手冊說」必須分開標註
曾把教師手冊的建議（進位記在下方）寫成「課本明訂」，
而課本 p21 明文其實是記在**上方**，方向相反。
這種錯會直接教錯學生。

## P7　測試失敗先查是產品壞了還是測試壞了
端到端第一次跑有兩個 FAIL，兩個都是**測試寫錯**：
- 最後一題成功訊息被整段覆寫，不再包含「答對」，但測試還在找「答對」
- 斷言 `'課本規定' not in body`，但頁面說明文字本身就有「不寫成『課本規定』」這句

**斷言要對著真正的要求寫**（例：三處出處都列出來），不要對著某個字串是否出現寫。

## P8　`startMerge()` 會清空圈選，端到端要重新點
`array.html` 階段二做完第一組會自動切到第二組並重置輸入框，
測試必須重新圈選才會出現輸入框，不能只填數字。
