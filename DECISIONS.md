# DECISIONS — 乘法直式板

## D1　核心邏輯抽成獨立 .js，網頁與測試共用同一份
沿用 U1 的結論。以前用 `node -e eval` 挖 `<script>` 出來跑，測的是複本，換行一改就壞。

## D2　用「進位格」消歧義，不用判定順序
只看寫入格時，「漏進位」與「先加後乘」有 **80 組碰撞**（條件 `(進位×乘數)%10===0`）。
第二輪審查建議用固定順序決定。**駁回**：
`OMIT_CARRY` 有教師手冊明文證據，另兩者沒有任何教材證據，
順序法等於讓無證據的假設蓋過有證據的迷思。
改成兩格一起判定 —— 窮舉 80,000 組後歧義為 0。

## D3　被乘數該位為 0 時不設兩個錯誤代碼
`(0＋進位)×乘數` 與 `進位×乘數` **是同一個算式**，
「拿進位去乘」與「先加後乘」在數學上無從分辨。
合併為 `ZERO_DIGIT_CARRY_CONFUSION`，回饋寫成兩種讀法都成立的一句話。
硬要分等於替學生的想法下一個沒有根據的斷言。

## D4　「先加後乘」不寫進 units.json 當迷思概念
它沒有任何教材證據，違反專案硬性規則「不可用推測補迷思概念」。
但它是程式端可偵測的輸入特徵，因此**只在回饋文字中處理**，
且措辭不得暗示「這是常見錯誤」。

## D5　二位數以上改列層級輸入，答錯才展開該列
逐位開進位格在 iPad 上會把畫面塞滿。取捨：診斷粒度平時降為「列」層級，
**答錯時才展開成逐位**。
附帶效果：這是本專案目前最接近真鷹架的機制（診斷 → 適切協助），
但它仍缺「依錯誤類型給不同協助」這一環，**所以還是不叫鷹架**。

## D6　L1/L2/L3 一律稱「協助遞減 / fading」，不稱鷹架
兩輪審查都建議叫「鷹架淡出」。**駁回命名，採納機制**。
專案定義的真鷹架須含診斷 → 適切協助 → 淡出與責任移轉三要素，
固定三關沒有診斷環節。

## D7　`array.html` 用表徵提示遞減，不用計算格線遞減
L1 完整四分割＋標註 → L2 只有外框 → L3 無圖反思題。
若照抄 `carry.html` 的格線／進位格遞減，`array.html` 會退化成第二個 `carry.html`，
**M6 面積模型連結完全消失**，而那是本單元唯一「紙本真的做不到」的一項。

## D8　`steps` 與 `parcel` 只宣稱是數位化練習
設計流程第 6 步判定這兩頁的微技能「印在學習單上效果一樣好」。
納入的理由是**單元覆蓋完整性**（活動一與開門活動不能沒有對應頁面），
不是「紙本做不到」。因此**不可宣稱它們在補救迷思概念**。

## D9　`steps` 不設輸入與診斷格
`steps` 與 `carry` 都用到 1426×3 與 2009×6，靠**互動形式**區分：
`steps` 是引導式逐步揭示（只看），`carry` 是主動輸入＋診斷。
否則學生在兩頁之間感受不到差異。

## D10　考古題一律用考卷原始選項
初稿曾用 `noShiftValue()` 算出誘答填進 110-1，違反「不可推測」。
改為照考卷原題（9624／8184／2424／984）。
誘答成因沒有官方命題規格可查，回饋只給**檢查方向**，不替學生斷言錯因。

## D11　全教具貫穿「幾個千、百、十、一重複幾次（幾倍）」位值表徵，並補足活動二「一、二位數×二位數」
乘法直式的本質是各個位值單位的重複計數（例如 6 個一的 8 倍＝48 個一；6 個一的 2 個十倍＝12 個十＝120）。
在 `steps.html` 擴充活動二（6×28、24×12、28×74）的兩列分解與「省略 0 / 補 0」切換，並在 `carry.html`、`array.html`、`fix.html` 各頁全面強化「幾個千、百、十、一 乘以幾倍 / 重複幾次」的語意表徵，建立一貫的位值心智模型。

## D12　採用「重複個位數次的在第一行，重複次數是十次的在第二行」統一二位數乘數直式講解
學生常因死記「先乘個位、再乘十位、第二行空一格」而無法理解兩列直式的本質，容易在第二列未左移（NO_SHIFT）犯錯。
採用「重複個位數次的在第一行，重複次數是十次的在第二行（對齊十位往左移一位），最後兩行合起來」：
1. 嚴格鎖定乘法「單位量 × 重複次數」的定義。
2. 直觀解釋了「直式為什麼有兩行」以及「第二行數值末位必為 0、為什麼要往左移一位」。
3. 統一適用於 1 位數×2 位數、2 位數×2 位數與更多位數乘法。
在 `steps.html`、`carry.html`、`array.html`、`fix.html` 全站兩列直式之說明、標籤與診斷回饋中全面採用。

## D13　`steps.html` 獨立拆出「✏️ 乘以二位數」分頁，且兩列直式每層逐位呈現（非整層倒出）
現場教師教學「四位數×一位數」（活動一）與「一、二位數×二位數」（活動二）分屬不同教學節次，分開為獨立分頁（`✏️ 乘以一位數`、`✏️ 乘以二位數`）避免教學時被迫逐題跳過。
此外，直式運算的認知過程是「逐位運算與進位」，直接整層跳出（例如直接出現 112 或 196）跳過了學童心智模型最核心的逐位乘與進位累積。改由 `multiDigitSteps` 將二位數直式拆解為個位乘、十位乘（對齊十位）與兩行相加之逐位呈現，搭配定位板即時高亮，並提供「← 上一步」按鈕供教師課堂回溯講解。


