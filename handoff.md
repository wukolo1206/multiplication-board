# 工作交接 — 2026-09-13（緊湊進位空間更貼近實際運算 v1.1.8）

## 已完成
- 完成 v1.1.8 直式進位行距與空間緊湊化，精準落實使用者教學指引：
  1. **進位空間大幅收緊（密一點）**：
     - 徹底修正進位列中 `place-spacer` 繼承 `.practice-cell` 預設 56px 高度的問題，全數更新為 `.mini-carry-cell.place-spacer`，將進位行高精確壓縮至 15px~20px。
     - 第一列與第二列之間的間隙由原先 64px 縮減至約 18px，第二列小進位 `⑤` 緊貼數字頭頂；加法進位 `+1` 緊靠第一列上面的直線下緣，視覺比例高度貼近真實黑板與紙筆手算直式。
  2. **全套測試 100% 通過**：
     - `node multiplication-core.test.js`：116 項核心單元測試全數通過（0 失敗）。
     - `python run-tests.py`：全站 6 個頁面端到端測試全部通過（Playwright 59 項驗證無 pageerror／console error）。

## 目前進度
進位空間緊湊化已完成並通過端到端與單元測試。

## 未完成／待確認
- 學校現場大屏與學生平板操作手感驗收。
- 是否需要推送更新至 GitHub Pages。

## 下一步
1. 請使用者確認是否需推送（push）至 GitHub Pages。
2. 學校現場大屏與學生平板實機教學驗收。

## 注意事項
- 本資料夾是獨立 Git repository；提交時保持專案邊界清晰。

---

# 工作交接 — 2026-09-13（三大進位位置分離 ＆ 全程保留進位 v1.1.7）

## 已完成
- 完成 v1.1.7 直式進位視覺與認知架構升級，完美落實使用者四大指示：
  1. **第二行的進位在第二列數字的上面**：
     - 第二列乘十位乘法進位（Phase 1，`r1`）獨立配置於「第一列與第二列之間」的緊湊進位列（`.mini-carry-cell.r1-carry-cell`），直接浮現在第二列各欄位數字頭頂。
  2. **進位小一點（`.mini`）**：
     - 第二列進位氣泡縮小為 18px 圓角徽章（字體 11px），加法進位縮小為 18px（字體 10.5px），緊湊小巧不佔版面，與頂部 22px 第一列進位形成清晰主從階層。
  3. **最後相加的進位在第一列上面的直線處**：
     - 兩排相加加法進位（Phase 2，`add`，如 `+1`）配置於第一列上方、緊靠橫線 1 下緣（`.add-carry-cell`），完美符合直式加法時將進位記在被加數（第一列）上方直線處的數學書寫習慣。
  4. **請都保留進位（全程不清除、不覆蓋）**：
     - 各階段進位（被乘數上方 Phase 0 進位、第一列上方直線處 Phase 2 加法進位、第二列數字上方 Phase 1 進位）在步驟推進與答對完成（`isDone`）後**全數保留顯示**；已使用進位轉為帶刪除線的淡化樣式，完整保留學生全題的進位思維脈絡。
  5. **全套測試 100% 通過**：
     - `node multiplication-core.test.js`：116 項核心單元測試全數通過（0 失敗）。
     - `python run-tests.py`：全站 6 個頁面端到端測試全部通過（Playwright 59 項驗證無 pageerror／console error）。

## 目前進度
三大進位位置分離、第二列上方小進位、加法進位於直線處、全程保留進位均已完成，全站測試通過。

## 未完成／待確認
- 學校現場大屏與學生平板操作手感驗收。
- 是否需要推送更新至 GitHub Pages。

## 下一步
1. 請使用者確認是否需推送（push）至 GitHub Pages。
2. 學校現場大屏與學生平板實機教學驗收。

## 注意事項
- 本資料夾是獨立 Git repository；提交時保持專案邊界清晰。

---

# 工作交接 — 2026-09-13（被乘數整體一體化外框 ＆ 正在算位數底色高亮 v1.1.6）

## 已完成
- 完成 v1.1.6 視覺與認知設計升級，精準落實使用者兩大核心教學指引：
  1. **被乘數整體框起來（一體化包覆外框）**：
     - 在兩列直式與二三位數×整十中，多位數被乘數（如 $52$、$42$、$375$）的所有位數共同包覆於一體化連續琥珀金圓角邊框（`.factor-a-group`）中，代表被乘數整體重複多次的計算本質。
     - 格線拼合技術：左端單元格（`group-start`）帶左圓角與無右邊框，中間單元格（`group-mid`）無左右邊框，右端單元格（`group-end`）帶右圓角與無左邊框，完美克服 CSS Grid 間隙，呈現無縫整體外框。
  2. **正在算的數位才用底色**：
     - 微步驟計算時，在被乘數整組外框內，**僅當前正在計算的數位**（如 $52 \times 94$ 中 $2 \times 4$ 的個位 `2`）亮起鮮黃底色（`#fef08a`，`.active-calc`）與深琥珀粗體字。
     - 其他位數（如十位 `5`）保持淡白底色無背景，直到下一步算 $5 \times 4$ 時，底色才動態平滑轉移至 `5`，`2` 褪去底色，焦點主次分明。
  3. **二三位數×整十全面同步**：
     - `steps.html` 的「二三位數×整十」直式展示（如 $42 \times 60$）同步將被乘數 `42` 一體化框起，乘數 `6` 獨立高亮，$0$ 標註固定個位。
  4. **全端與雙頁面一致**：
     - `steps.html`（兩列直式分頁與二三位數×整十）與 `array.html`（兩列直式練習分頁）同步更新。
  5. **全套測試 100% 通過**：
     - `node multiplication-core.test.js`：116 項核心單元測試全數通過（0 失敗）。
     - `python run-tests.py`：全站 6 個頁面端到端測試全部通過（Playwright 59 項驗證無 pageerror／console error）。

## 目前進度
被乘數一體化包覆外框與正在算位數動態底色高亮已全面落實並通過端到端與單元測試。

## 未完成／待確認
- 學校現場大屏與學生平板操作手感驗收。
- 是否需要推送更新至 GitHub Pages。

## 下一步
1. 請使用者確認是否需推送（push）至 GitHub Pages。
2. 學校現場大屏與學生平板實機教學驗收。

## 注意事項
- 本資料夾是獨立 Git repository；提交時保持專案邊界清晰。

---

# 工作交接 — 2026-09-13（兩列直式微步逐位算含進位符號 ＆ 非焦點數字調亮 v1.1.5）

## 已完成
- 完成 v1.1.5 升級，全面落實使用者兩大核心需求：
  1. **按一下就是一個微步驟（含進位符號視覺化）**：
     - 利用 `C.multiDigitSteps(a, b)`，將兩列直式（以 $12 \times 95$ 為例）完整拆解為 9 個微步驟：個位乘個位 $\to$ 十位乘個位 $\to$ 個位乘十位 $\to$ 十位乘十位 $\to$ 兩排逐位相加。
     - 每按一下只計算並填入一個數字，直式定位板專屬配置「進位列」（`.carry-cell`）：
       - 第一列進位：橘黃色徽章（`r0`，如 `①`、`④`），加完後轉為帶刪除線的 `.used`。
       - 第二列進位：藍紫色徽章（`r1`）。
       - 兩排相加進位：珊瑚紅徽章（`add`，如 `+1`）。
     - 當前相乘的被乘數位與乘數位獨立亮起，直式目標格以紫色虛線框動態鎖定。
     - 右側引導卡與下方按鈕同步顯示精細微步指引（如「👉 點我填入：個位寫 2，十位記進位 1」）。
  2. **乘數非焦點灰色數字調亮**：
     - 乘數未參與當前計算的數字由原本過暗的 `opacity: 0.2` 調亮至 `opacity: 0.5`（`#64748b`），大螢幕與平板上清晰可讀，同時維持主從焦點層次。
  3. **雙模式自由切換**：
     - 頂部提供 `[ 👣 逐位算（含進位） ]`（預設，按一下就是一個微步並顯示進位）與 `[ ⚡ 整列算 ]`（快速模式，一次填入整列），同步支援 `steps.html` 與 `array.html`。
  4. **全套測試 100% 通過**：
     - `multiplication-core.test.js`：116 項核心單元測試全數通過。
     - `python run-tests.py`：全站 6 個頁面端到端測試全部 100% 通過。

## 目前進度
兩列直式微步驟拆解、進位符號視覺化與非焦點字調亮均已完成，`steps.html` 與 `array.html` 雙頁面同步升級並通過全套自動化測試。

## 未完成／待確認
- 學校現場大屏與學生平板操作手感驗收。
- 是否需要推送更新至 GitHub Pages。

## 下一步
1. 請使用者確認是否需推送（push）至 GitHub Pages。
2. 學校現場大屏與學生平板實機教學驗收。

## 注意事項
- 本資料夾是獨立 Git repository；提交時保持專案邊界清晰。

---

# 工作交接 — 2026-09-13（array.html 步驟式高亮與點選填入 v1.1.2）

## 已完成
- `array.html` 練習模式全面升級為「步驟式焦點高亮與點選填入（一次強調一個重點）」：
  - **強調第一步要乘的**：被乘數每一格與乘數個位在第一步以亮黃金框與微光暈顯著強調；第二步切換為乘數十位高亮。
  - **乘數另一個數字變暗**：第一步時乘數十位變暗（低對比淡灰色）；第二步時乘數個位變暗，避免學生分心。
  - **文字出現在各列旁邊**：直式 Grid 將引導說明列水平對齊各列高度，第一列、第二列與合計列各自緊貼專屬的說明文字與按鈕。
  - **直接點選不需鍵盤打字**：取消 `<input>` 輸入框，改為點選式答案格與步驟按鈕，點擊右側按鈕或直式格子即可直接填入對應位值欄位。
  - **一次強調一個重點**：分步鎖定推進（第 1 步個位列 → 第 2 步十位列，強調非乘 2 而是乘 20 → 第 3 步兩排相加 → 答對加總確認），上方口語提示句與下方重複次數等式同步動態對齊焦點。
- 測試全部通過：
  - `node multiplication-core.test.js`：106 項通過。
  - `python run-tests.py`：端到端測試全部通過（含定位板格線對齊、高亮與變暗驗證、分步填入與無限練習）。

## 目前進度
v1.1.2 互動功能已完成，核心測試與端到端測試 100% 通過。

## 未完成／待確認
- 尚未在學校的教師大屏、學生平板與實際瀏覽器環境做現場驗收。

## 下一步
1. 到學校用大屏與學生平板實測點選填入、欄位對齊、變暗高亮對比與口語提示。
2. 收集學生操作回饋後，確認是否需要調整音效或按鈕大小。

## 注意事項
- 第二行一定要顯示實際十位值：`×10`、`×20`，不可退回 `×1`、`×2`。
- 本資料夾是獨立 Git repository；提交時保持專案邊界清晰。

---

# 工作交接 — 2026-09-13（乘法直式板 v1.1.1）

## 本次完成

- `array.html` 練習頁改成無限隨機出題，可選被乘數一至四位數，預設兩位數。
- 第一列使用個位重複次數；第二列使用實際十位值（×10、×20…），並在大字直式中對齊十位。
- 畫面加入口語記憶句、重複次數加總等式、分步錯誤提示與已練習題數。
- 依實際畫面回饋修正為真正的定位板 Grid：百／十／個位固定欄位，答案每格填一個數字，第二列與其他列共用欄位中心線。
- 保留探索與挑戰分頁，未改變其既有流程。

## 目前狀態

功能已完成並部署至既有 GitHub Pages；遠端 `main` 與本機提交 `59c4396` 一致，線上 `array.html` 已驗證為 v1.1.1 定位板新版。

## 本次驗證

- `node multiplication-core.test.js`：106 項通過。
- Node Playwright：一至四位數、×10／×20 實際十位列、第二列左移、答題流程、無限下一題、桌面與 390px 窄版通過，無 pageerror／console error。
- `python run-tests.py`：目前環境的 Python 沒有 `playwright` 套件，未能執行原有 Python 端到端入口；已用可用的 Node Playwright 完成同等互動驗收。

## 下次接續

1. 到學校用教師大屏與學生 iPad 實機驗收大字、輸入框與第二列十位對齊。
2. 觀察學生是否能依口語句說出「個位第一列、十位實際值第二列、重複次數加總」。

## 注意事項

- 第二列文案必須寫實際值 `×10`、`×20` 等，不要退回 `×1`、`×2`。
- 第二列的 `data-shift="1"` 與 CSS 左移是位值教學契約；若改版要同步更新端到端驗收。
- 乘數十位與個位目前排除 0，避免本練習混入「只有一列有效」的特殊題型。

---

# 工作交接 — 2026-08-23（乘法直式板 v1.0）

## 已完成

四上第二單元「整數的乘法」互動教具，五頁＋導覽，**已發佈上線**。

🌐 https://wukolo1206.github.io/multiplication-board/

### 測試
```bash
python run-tests.py
```
核心 74 項 ＋ 端到端 52 項，全部通過。

### 走過的流程
`docs/互動教具設計流程.md` 九步全走完：
- 閘門原本**未通過**（U2 三筆迷思全是誘答假設），從教師手冊補入三筆明文證據後通過
- 課本 12 頁逐頁讀完，拆出任務序列
- 微技能 12 條，第 6 步篩選出「紙筆真的做不到」的四項
- 設計文件 v1.2，經**兩輪交叉審查**共 23 條（採納 17、部分採納 6、駁回 0）

### 兩輪審查抓到的重點
- 第一輪：一條**會直接教錯**的錯誤 —— 原文寫「課本 p25 明訂進位記在下方」，
  實際課本 p21 明文是**上方**，「下方」是教師手冊的建議
- 第二輪：0 條內容錯誤，9 條全是**第一輪修正自己引入的**演算法缺陷
  （100% 死碼、比對完整乘積而非寫入個位數、列層級輸入沒有對應診斷、
  計算型協助遞減誤套到 array 會摧毀 M6）

## 目前進度

| repo | commit | 狀態 |
|---|---|---|
| knowledge-map | `5374295` | 已推送，遠端 SHA 已驗證 |
| beike-ai 上層 | `50d2f8c` | 已推送（gitlink 推進 ＋ 設計文件） |
| multiplication-board | `81ea0fd` | 已推送（橫式練習同步呈現對應直式與定位板、打字即時同步位值映射、94/94 單元測試通過），Pages 部署中 |

## 未完成／待確認

- 🔴 **實機驗收**：教師觸控大屏與學生 iPad 的實際手感（拖曳靈敏度、字級、48px 是否真的夠大）
  —— 這是唯一只有現場才能做的事
- 🟡 **L3 無協助正確率沒有收集機制**。設計文件訂了「L3 正確率才是這教具有沒有用的證據」，
  但目前沒有任何紀錄方式，只能靠老師現場觀察。這是 U1 就留下的問題
- 🟡 **真鷹架仍未實作**。`carry.html` 的「答錯才展開該列」是目前最接近的機制
  （有診斷、有適切協助，但缺依錯誤類型分層），
  可作為 U1 handoff 提到的**最小試辦**候選 —— 但要與無協助表現比較，沒增益就停止擴建
- 🟡 `array.html` 的 L2「只有外框」連標號都拿掉了。
  若現場發現學生完全無從下手，再考慮保留標號但拿掉面積數字
- 🟡 **既有問題（非本次造成）**：`班級計時器` 的 gitlink 指向遠端不存在的 SHA，
  修法：`git -C 班級計時器 push`
- 🟢 已完成：GitHub repo（public）、push、Pages、上層 gitlink 與 `.gitmodules`、
  `units.json` 的 `tools`、`康軒數學/CLAUDE.md`、`專案總表.md`、
  方法心得寫回 `docs/互動教具設計流程.md`

## 注意事項

### 這次踩到的（詳見 PITFALLS.md）
- 依序比對的規則集會出現死碼，**寫完就要窮舉跑一次**
- 改了版面（列層級輸入）就要回頭檢查判定邏輯還對不對
- 一套機制套到第二個頁面前，先問會不會摧毀那一頁的目的
- 教材頁碼要雙標；「課本說」與「教師手冊說」必須分開
- 端到端兩個 FAIL 都是**測試寫錯**，不是頁面壞

### 專案慣例
- 改核心邏輯後務必 `python run-tests.py`
- 不要把邏輯搬回 HTML 內嵌
- 不要把協助遞減叫成鷹架
- 考古題選項照考卷原題，不可自己算誘答
- `core.autocrlf=true` 造成大量假的「已修改」，以 `git diff` 為準不是 `git status`
